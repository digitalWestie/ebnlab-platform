<p style="color: green"><%= notice %></p>

<%= render @problem %>

<div>
  <%= link_to "Edit this problem", edit_project_problem_path(@project, @problem) %> |
  <%= link_to "Back to problems", project_problems_path(@project) %> |
  <%= link_to "Show versions", versions_problem_path(@problem) %> |

  <%= button_to "Destroy this problem", project_problem_path(@project, @problem), method: :delete %>
</div>
