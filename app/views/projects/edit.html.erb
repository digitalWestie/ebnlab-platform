<h1 class="opacity-20 text-2xl sm:text-3xl md:text-4xl lg:text-5xl mr-6 mb-6">Edit project</h1>

<div class="flex flex-row items-center justify-between mb-10">
  <h2 class="text-xl sm:text-2xl md:text-3xl lg:text-4xl"><%= @project.name %></h2>

  <%= link_to [@organisation, @project], class: "cursor-pointer text-base sm:text-lg inline-block border border-gray-600 bg-gray-600 text-white rounded-md px-4 py-2 transition duration-500 ease select-none hover:bg-gray-700  focus:shadow-outline flex flex-row items-center" do %>

    <span>Cancel edit</span>
  <% end %>
</div>

<%= render "form", project: @project %>
